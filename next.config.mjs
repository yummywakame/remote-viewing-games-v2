/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true,
    },
    // Preserve any existing configuration options you might have
  }
  
  export default nextConfig